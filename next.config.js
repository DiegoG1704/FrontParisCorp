/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Habilita la exportación estática
    trailingSlash: true, // Agrega una barra final en las URLs (opcional, pero recomendado)
  };
  
  export default nextConfig;
  